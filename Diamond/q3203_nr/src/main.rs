use std::io;
use io::stdin;
use io::stdout;

fn add() {

}

fn repeat() {

}

fn print() {

}

fn main() {
    
    let mut variable : [usize ; 26] = [0 ; 26];
    let stdin = std::io::stdin();
    let mut _in = io::BufReader(stdin.lock());
    let stdout = std::io::stdout();
    let mut _out = io::BufWriter(stdout.lock());



}
